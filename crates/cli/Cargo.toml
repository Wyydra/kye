[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
domain = { path = "../domain" }
infra = { path = "../infra" }
tokio = { version = "1.50.0", features = ["rt-multi-thread", "macros"] }
anyhow = "1"
